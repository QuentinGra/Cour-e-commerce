{{ form_start(form) }}
{{ form_row(form.name) }}
{% include 'Backend/Product/_productImage.html.twig' %}
{{ form_widget(form) }}
<button class="btn" type="submit">Modification</button>
{{ form_end(form) }}
