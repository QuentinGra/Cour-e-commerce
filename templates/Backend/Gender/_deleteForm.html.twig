<form action="{{ path('admin.genders.delete', {id: gender.id}) }}" method="post">
	<input type="hidden" name="token" value="{{ csrf_token('delete' ~ gender.id) }}">
	<button class="btn" type="submit">Supprimer</button>
</form>
